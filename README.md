# E-commerce Data Warehouse & Recommendation System

This project implements an end-to-end data pipeline that transforms raw e-commerce data into a structured data warehouse, generates product recommendations using SQL, and exposes insights through an interactive Streamlit dashboard.

The repository includes:

* ETL pipeline (Python)
* Star schema data warehouse (MySQL)
* Analytical SQL queries
* Streamlit application
